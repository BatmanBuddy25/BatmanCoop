@page "/transactionlogs-list"

@rendermode InteractiveAuto


<PageTitle>Member Buyer List</PageTitle>




<FluentDataGrid Items="@IList_TransLogs"
                Pagination="@pagination"
                ShowHover="true"
                GenerateHeader="GenerateHeaderOption.Sticky"
                Style="height: 100%;overflow:auto;">

    <ChildContent>
        <PropertyColumn Property="@(a => a.MemM!.MemberNo)" Title="Member No." Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.MemM!.LastName +", "+ a.MemM!.FirstName)" Title="Name" Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.MemM!.Bank_Number)" Title="Payment No." Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.BuyM!.Buy_Code)" Title="Code" Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.BuyM!.Share_Capital)" Title="Capital" Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.Buy_Amount)" Title="Amount" Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.Created_Date.ToShortDateString())" Title="Date" Width=".5fr" Sortable="true" />
        <PropertyColumn Property="@(a => a.Trans_Status)" Title="Status" Width=".5fr" Sortable="true" />
    </ChildContent>
    <EmptyContent>
        <FluentStack Orientation="Orientation.Vertical" HorizontalAlignment="HorizontalAlignment.Center">
            <div> No Data Found</div>
        </FluentStack>
    </EmptyContent>
</FluentDataGrid>
<FluentPaginator State="@pagination" />


